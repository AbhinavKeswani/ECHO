# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/

# ECHO local state (audio cache, DB copies, exported vault)
/audio_cache/
/vault_export/
*.db
*.db-wal
*.db-shm

# Library snapshots contain your personal liked-songs data + analysis — never publish
# to the public code repo. Sync these through a PRIVATE repo/branch instead.
*.snapshot.json.gz
/data/

# Secrets
.env
